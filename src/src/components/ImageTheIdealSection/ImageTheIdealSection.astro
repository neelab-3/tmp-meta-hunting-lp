---
import { Image } from "astro:assets";
import Ideal from "./Ideal.astro";
import front from "../../assets/image-the-ideal-section/image-the-ideal-front--modified.png";
import back from "../../assets/image-the-ideal-section/ManOfImageTheIdealSection--minified.png";
---

<section id="image-the-ideal-section">
  <div class="corner-border">
    <h2 class="image-the-ideal-title">
      あなたの就職活動、
      <br />
      こんな"理想"はありませんか？
    </h2>
    <div class="bottom-corners"></div>
  </div>
  <div class="image-the-ideal-content">
    <div class="ideal-wrapper">
      <Ideal text="会社のリアルな雰囲気を知ってから決めたい" color="#22B29A" />
      <Ideal
        text="自分の可能性が広がる場所で挑戦したい"
        color="#DF568E"
        ml="7rem"
      />
      <Ideal text="まだ知らない優良な企業に出会いたい" color="#22B29A" />
      <Ideal
        text="どうせなら都会で働いて視野を広げたい"
        color="#DF568E"
        ml="7rem"
      />
      <Ideal text="上京という選択肢を諦めたくない" color="#22B29A" />
      <Ideal
        text="最初の就職で絶対に失敗したくない"
        color="#DF568E"
        ml="7rem"
      />
      <Ideal
        text="心から「この会社が良い」と思える企業に出会いたい"
        color="#22B29A"
      />
    </div>
    <div>
      <Image src={front} alt="" class="img front" loading="lazy" />
      <Image src={back} alt="" class="img back" loading="lazy" />
    </div>
  </div>
</section>
<style>
  #image-the-ideal-section {
    position: relative;
    padding: 60px 20px;
    text-align: center;
    background: radial-gradient(
        ellipse 600px 400px at 15% 20%,
        #64b5f6 0%,
        transparent 40%
      ),
      radial-gradient(
        ellipse 500px 400px at 60% 15%,
        /* 60%位置 */ #ba68c8 0%,
        transparent 45%
      ),
      radial-gradient(
        ellipse 600px 400px at 85% 20%,
        #1565c0 0%,
        transparent 40%
      ),
      radial-gradient(
        ellipse 600px 400px at 15% 80%,
        #ab47bc 0%,
        transparent 40%
      ),
      radial-gradient(
        ellipse 500px 400px at 50% 90%,
        #1976d2 0%,
        transparent 45%
      ),
      radial-gradient(
        ellipse 600px 400px at 85% 80%,
        #42a5f5 0%,
        transparent 40%
      ),
      linear-gradient(135deg, #5e35b1 0%, #1e88e5 100%);
    overflow: hidden;
  }

  .image-the-ideal-title {
    margin: 0;
    color: white;
    font-size: 2.3rem;
    font-weight: bold;
    line-height: 1.3;
  }

  /* @NOTE: 角のボーダー */
  .corner-border {
    position: relative;
    display: inline-block;
    padding: 40px 60px;
  }

  .bottom-corners {
    position: absolute;
    width: 100%;
    height: 100%;
    top: 0;
    left: 0;
    pointer-events: none;
  }

  .corner-border::before,
  .corner-border::after {
    content: "";
    position: absolute;
    width: 30px;
    height: 30px;
    border: 3px solid white;
  }

  .corner-border::before {
    top: 0;
    left: 0;
    border-right: none;
    border-bottom: none;
  }

  .corner-border::after {
    top: 0;
    right: 0;
    border-left: none;
    border-bottom: none;
  }

  .bottom-corners::before,
  .bottom-corners::after {
    content: "";
    position: absolute;
    width: 30px;
    height: 30px;
    border: 3px solid white;
  }

  .bottom-corners::before {
    bottom: 0;
    left: 0;
    border-right: none;
    border-top: none;
  }

  .bottom-corners::after {
    bottom: 0;
    right: 0;
    border-left: none;
    border-top: none;
  }

  .image-the-ideal-content {
    margin: 2rem auto 0 auto;
    width: 90%;
    max-width: 1600px;
    display: flex;
    gap: 2rem;
    justify-content: center;
  }

  .ideal-wrapper {
    position: relative;
    max-width: 800px;
    margin: 0 auto;
    flex: 1;
  }

  .image-the-ideal-content > div:last-child {
    position: relative;
    flex-shrink: 0;
    width: 400px;
    height: 500px;
  }

  .img {
    width: 400px;
    height: 500px;
    margin: 0;
    object-fit: contain;
    clip-path: polygon(
      /* 左上 */ 0% 0%,
      /* 右上 */ 100% 0%,
      /* 右下（斜め） */ 97% 100%,
      /* 左下（斜め） */ 0% 100%
    );
  }

  .front {
    position: absolute;
    bottom: 2.5rem;
    right: 10rem;
    z-index: 1;
  }

  .back {
    position: absolute;
    height: 400px;
    bottom: -1rem;
    right: -4rem;
    z-index: 0;
  }

  @media (max-width: 1100px) {
    .front {
      right: 5rem;
    }
  }

  @media (max-width: 1024px) {
    .image-the-ideal-content {
      flex-direction: column;
      align-items: center;
      gap: 0;
    }

    .ideal-wrapper {
      width: 100%;
    }

    .image-the-ideal-content > div:last-child {
      position: relative;
      width: 100%;
      height: 500px;
    }

    .image-the-ideal-title {
      font-size: 2rem;
    }

    .corner-border {
      padding: 30px 40px;
    }

    .front {
      right: 5rem;
      bottom: 2rem;
    }

    .back {
      right: -5rem;
    }
  }

  @media (max-width: 768px) {
    #image-the-ideal-section {
      padding: 40px 15px;
    }

    .image-the-ideal-title {
      font-size: 1.5rem;
    }

    .corner-border {
      padding: 20px 30px;
    }

    .corner-border::before,
    .corner-border::after,
    .bottom-corners::before,
    .bottom-corners::after {
      width: 20px;
      height: 20px;
      border-width: 2px;
    }

    .image-the-ideal-content {
      width: 95%;
    }
  }

  @media (max-width: 480px) {
    .image-the-ideal-title {
      font-size: 1.2rem;
    }

    .corner-border {
      padding: 15px 20px;
    }

    .image-the-ideal-content > div:last-child {
      height: 350px;
    }

    .img {
      width: 280px;
      height: 350px;
    }

    .front {
      right: 3rem;
      bottom: 1rem;
    }

    .back {
      right: -3rem;
      height: 280px;
      bottom: -6rem;
    }
  }
</style>
