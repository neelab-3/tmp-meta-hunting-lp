<h2 id="hero-section--catch-phrase">スマホひとつで、就職の未来が変わる。</h2>
<style>
  #hero-section--catch-phrase {
    font-size: 0.9rem;
    text-align: center;
    clip-path: polygon(
      /* 左上 */ 3% 0%,
      /* 右上 */ 100% 0%,
      /* 右下（斜め） */ 97% 100%,
      /* 左下（斜め） */ 0% 100%
    );
    display: block;
    width: fit-content;
    max-width: 100%;
    line-height: 1.3;
    margin: 0 auto;
    padding: 5px 0.6rem 5px 0.8rem;
    color: white;
    background: linear-gradient(
      to right,
      /* 左 */ rgb(49, 179, 174),
      /* 中央 */ rgb(0, 43, 178),
      /* 右 */ rgb(129, 35, 91)
    );
  }

  @media (min-width: 332px) {
    #hero-section--catch-phrase {
      white-space: nowrap;
    }
  }

  @media (min-width: 480px) {
    #hero-section--catch-phrase {
      font-size: min(3.2vw, 1.5rem);
      padding: 5px 1rem 5px 1.2rem;
    }
  }

  @media (min-width: 768px) {
    #hero-section--catch-phrase {
      font-size: 2.2rem;
      padding: 5px 1.5rem 5px 1.8rem;
    }
  }

  @media (min-width: 1024px) {
    #hero-section--catch-phrase {
      font-size: 3rem;
      line-height: 1.1;
      padding: 5px 2rem 5px 2.5rem;
    }
  }
</style>
